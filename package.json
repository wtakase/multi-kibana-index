{
  "name": "multi_kibana_index",
  "version": "0.0.0",
  "description": "This enables to support multiple kibana.index. User can access own kibana.index and also group kibana.index by selecting on the plugin interface. kibana.index list will be generated based on username and LDAP roles.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint",
    "start": "plugin-helpers start",
    "test:server": "plugin-helpers test_server",
    "test:browser": "plugin-helpers test_browser",
    "build": "plugin-helpers build",
    "postinstall": "plugin-helpers postinstall"
  },
  "dependencies": {
    "ldapjs": "^1.0.0",
    "yar": "^7.0.2"
  },
  "devDependencies": {
    "@elastic/eslint-config-kibana": "0.0.2",
    "@elastic/plugin-helpers": "5.0.0-beta1",
    "babel-eslint": "4.1.8",
    "eslint": "1.10.3",
    "eslint-plugin-mocha": "1.1.0",
    "ldapjs": "^1.0.0",
    "yar": "^7.0.2"
  }
}
